name: Flutter CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1) Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2) Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      # 3) Check Flutter version (optional but nice for logs)
      - name: Flutter version
        run: flutter --version

      # 4) Get dependencies
      - name: Flutter pub get
        run: flutter pub get

      # 5) Static analysis
      - name: Flutter analyze
        run: flutter analyze

      # 6) Run tests
      - name: Flutter tests
        run: flutter test

      # 7) Build Android APK (release)
      - name: Build Android APK (release)
        run: flutter build apk --release

      # 8) Build Web (release)
      - name: Build Web (release)
        run: flutter build web --release

      # 9) Upload APK as artifact
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      # 10) Upload Web build as artifact
      - name: Upload Web build
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: build/web
